<p id="notice"><%= notice %></p>

<h1><%= t('.show_task') %></h1>

<p>
<tr>
  <strong><%= t('.title') %></strong>
  <%= @task.title %></td><br>

  <td><strong><%= t('.content') %></strong>
  <%= @task.content %></td><br>

  <td><strong><%= t('.create_at') %></strong>
  <%= @task.created_at %></td>
</tr>
</p>


<%= link_to t('.edit'), edit_task_path(@task), id: "edit-task"  %> |
<%= link_to t('.back'), tasks_path, id: "back" %>